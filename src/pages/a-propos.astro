---
import EntryLayout from "@components/a-propos/EntryLayout.astro";
import { getIndex } from "@lib/contentParser";
import type { CollectionEntry } from "astro:content";

const aProposEntry = (await getIndex("a-propos")) as CollectionEntry<"a-propos">;
---

<EntryLayout entry={aProposEntry} />